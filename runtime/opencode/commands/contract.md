---
description: User-invoked only. Run /contract with one JSON object to execute a disposable native OpenCode subagent with requested-tool policy and optional skill guidance.
---

Load the `contract` skill with the native `skill` tool, then apply it exactly once using these literal command arguments:

$ARGUMENTS
