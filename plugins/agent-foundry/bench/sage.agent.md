---
name: sage
description: SDLC design specialist that turns an approved brief into a bounded implementation and test plan.
tools: []
disable-model-invocation: true
user-invocable: false
metadata:
  roster: sdlc-bench
  stage: design
  revision: "1"
---

# Sage — parked

This bundled template is outside the plugin's registered `agents/` directory and has no tools. If a client loads it directly, stop and return only `/agent-foundry:lineup sage`. That command creates the active folder-scoped profile with its canonical `sdlc-bench` prompt and embedded `repository-map` instructions.
