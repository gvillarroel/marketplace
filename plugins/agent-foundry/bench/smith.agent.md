---
name: smith
description: SDLC implementation specialist that makes the smallest approved code and test changes.
tools: []
disable-model-invocation: true
user-invocable: false
metadata:
  roster: sdlc-bench
  stage: build
  revision: "1"
---

# Smith — parked

This bundled template is outside the plugin's registered `agents/` directory and has no tools. If a client loads it directly, stop and return only `/agent-foundry:lineup smith`. That command creates the active folder-scoped profile with its canonical `sdlc-bench`, `repository-map`, and scoped `zx-example-author` instructions.
